import 'package:flutter/material.dart';

class AppStartModalSheet extends StatelessWidget {
  const AppStartModalSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
